apiVersion: v1
kind: Service
metadata:
  name: watchdog-service
  labels:
    name: watchdog-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002
  selector:
    name: watchdog